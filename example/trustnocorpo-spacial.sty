% trustnocorpo LaTeX Style (Example wrapper)
% This example package is a thin, branded wrapper around the core `tnc` package.
% It exposes trustnocorpo-branded helper macros while staying compatible.
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{trustnocorpo-spacial}[2025/09/09 TrustNoCorpo branded wrapper over tnc]

% Load the core package
\RequirePackage{tnc}

% Branded helper macros (use these in your documents)
%  \trustnocorpoClass{LEVEL}
%  \trustnocorpoWatermark{TEXT}
%  \trustnocorpoWatermarkSetup{opacity=40,angle=45,tile=false}
%  \trustnocorpoFooter{TEXT}
%  \trustnocorpoRecipient{TOKEN}
\newcommand{\trustnocorpoClass}[1]{\tncclass{#1}}
\newcommand{\trustnocorpoWatermark}[1]{\tncwatermark{#1}}
\newcommand{\trustnocorpoWatermarkSetup}[1]{\tncwatermarksetup{#1}}
\newcommand{\trustnocorpoFooter}[1]{\tncfooterfingerprint{#1}}
\newcommand{\trustnocorpoRecipient}[1]{\tncrecipient{#1}}

% Auto-inject metadata/watermark/footer invisibly at begin document via tnc
% (No extra code needed here; tnc does the heavy lifting.)

% Backward-compat aliases (legacy example names)
\newcommand{\capyEmbedCryptoInfo}{\tncEmbedMeta}
\let\capyApplyWatermark=\tncApplyWatermark
\let\capyApplyFooter=\tncApplyFooter

\endinput
